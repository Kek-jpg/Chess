from math import *
while True: # проверка ввода пользователя
                try:
                    k = int(input("Введите текущую координату фигуры (номер вертикали): "))
                    l = int(input("Введите текущую координату фигуры (номер горизонтали): "))
                    m = int(input("Введите координату для хода (номер вертикали): "))
                    n = int(input("Введите координату для хода (номер горизонтали): "))
                    if (k > 8 or l > 8 or m > 8 or n > 8):
                        raise Exception()
                    break
                except Exception as e:
                    print('Данные введены неверно. Введите число (не больше 8)')
#a
if ((k + l + m + n) % 2 == 0): # проверка полей на совпадение цветов
    print("а) Поля одного цвета")
else:
    print("а) Поля разных цветов")
#б
if abs(k-m) == abs(l-n) or k == m or l == n: # проверка хода ферзя
   print('б) Ферзь угрожает данному полю')
else:
    print('б) Ферзь не угрожает данному полю')
#в
if ((abs(abs(k-m)-2)<0.5) and (abs(abs(l-n)-1)<0.5) # проверка хода коня
    or (abs(abs(k-m)-1)<0.5) and (abs(abs(l-n)-2.0)<0.5)):
    print('в) Конь угрожает данному полю')
else:
    print('в) Конь не угрожает данному полю')
#г
if (k==m) or (l==n): # проверка хода ладьи
   print('г) Ладья может сделать ход')
else:
    print('г) Ладья не может сделать ход')
    print("Возможный вариант промежуточного хода: " + str(k) + " " + str(n))
    print("Возможный вариант промежуточного хода:  " + str(m) + " " + str(l))
#д
if abs(k-m) == abs(l-n) or k == m or l == n: # проверка хода ферзя
   print('д) Ферзь может сделать ход')
else:
    print('д) Ферзь не может сделать ход')
    print("Возможный вариант промежуточного хода: " + str(k) + " " + str(n))
    print("Возможный вариант промежуточного хода: " + str(m) + " " + str(l))
#е
if abs(k-m) == abs(l-n): # проверка хода слона
   print('е) Слон может сделать ход')
else:
    print('е) Слон не может сделать ход')
    if ((k + l + m + n) % 2 != 0):
        print("слон никогда не сможет попасть на это поле")
    else:
        print("Возможный вариант промежуточного хода:  " + str(int((k+l+m-n)/2)) + " " + str(int((k+l-m+n)/2)))
        print("Возможный вариант промежуточного хода: " + str(int((k-l+m+n)/2)) + " " + str(int((-k+l+m+n)/2)))